# হ্যাশট্যাগ মেকার GUI

### 👑 By RJ SHeesh

এটি একটি সাধারণ পাইথন GUI টুল, যা ইউটিউব, টিকটক বা অন্যান্য সোশ্যাল মিডিয়ার জন্য দ্রুত হ্যাশট্যাগ তৈরি করতে সাহায্য করে।

---

## 🌟 ফিচারসমূহ (Features)

* **দ্রুত হ্যাশট্যাগ জেনারেট:** যেকোনো বাক্য বা কীওয়ার্ডের লিস্ট পেস্ট করলে প্রতিটি শব্দকে (`word`) আলাদা হ্যাশট্যাগে পরিণত করে।
* **ডিফল্ট ট্যাগ:** আপনার দেওয়া নির্দিষ্ট কিছু ট্যাগ (যেমন `#viralvideo`, `#foryou`) সব সময় আউটপুটের শুরুতে স্বয়ংক্রিয়ভাবে যোগ হয়ে যায়।
* **ডুপ্লিকেট রিমুভার:** ডিফল্ট ট্যাগ এবং আপনার দেওয়া নতুন ট্যাগের মধ্যে কোনো ডুপ্লিকেট থাকলে তা বাদ দিয়ে দেয়।
* **মাল্টি-সেপারেটর:** কমা (,), স্পেস ( ) বা এন্টার (নতুন লাইন) যাই দিন না কেন, এটা সব শব্দকেই আলাদা করতে পারে।
* **ক্লিপবোর্ডে কপি:** জেনারেট করা সব হ্যাশট্যাগ এক ক্লিকে কপি করার জন্য "Copy Hashtags" বাটন আছে।

---

## 🚀 ব্যবহারবিধি (How to Use)

১. `hashtag_maker.exe` টি রান করান।
২. উপরের বড় বক্সে আপনার কীওয়ার্ড বা বাক্যগুলো পেস্ট করুন।
   * (যেমন: `bangla natok, apurbo, mehazabien new natok`)
৩. **"Generate Hashtags"** বাটনে ক্লিক করুন।
৪. নিচের বক্সটি আপনার ডিফল্ট ট্যাগ এবং নতুন জেনারেট হওয়া হ্যাশট্যাগ দিয়ে ভরে যাবে।
   * (যেমন: `#viralvideo #foryou ... #bangla #natok #apurbo #mehazabien #new`)
৫. **"Copy Hashtags"** বাটনে ক্লিক করে সব ট্যাগ কপি করে নিন।

---

## ⚙️ যা যা লাগবে (Requirements)

* **আর কিছু লাগবে না!** 
